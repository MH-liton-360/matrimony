const Favorites = () => {
    return (
        <section>

            <title>Favorites - Matrimony</title>

            <div>
                <h1>This is favorites</h1>
            </div>
        </section>
    );
};

export default Favorites;